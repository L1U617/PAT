#include "stdafx.h"
#include <iostream>
#include <vector>
/*
using namespace std;

int main()
{
	int n, time = 0;
	cin >> n;
	vector<int> v(n + 1);
	v[0] = 0;
	for (int i = 1; i <= n; i++)	{
		cin >> v[i];
		int move = v[i] - v[i - 1];
		if (move >= 0)
			time += (move * 6);
		else time += (abs(move) * 4);
		time += 5;
	}
	cout << time;
	system("pause");
	return 0;
}*/